version: '3.8'
services:
  nginx:
    image: nginx:latest
    container_name: nginx-homelab
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./html:/usr/share/nginx/html
      #- ./config:/etc/nginx/conf.d
